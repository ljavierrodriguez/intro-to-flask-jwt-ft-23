# Comando a Ejecutar para iniciar el proyecto

Creamos el entorno virtual
```shell
pipenv shell
```

Instalamos las dependencias
```shell
pipenv install
```

Iniciamos el Servicio 
```shell
python src/app.py
```